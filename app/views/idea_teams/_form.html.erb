<%= form_for(@idea_team) do |f| %>
  <% if @idea_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idea_team.errors.count, "error") %> prohibited this idea_team from being saved:</h2>

      <ul>
      <% @idea_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idea_id %><br>
    <%= f.number_field :idea_id %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br>
    <%= f.number_field :role_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
