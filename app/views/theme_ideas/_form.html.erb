<%= form_for(@theme_idea) do |f| %>
  <% if @theme_idea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theme_idea.errors.count, "error") %> prohibited this theme_idea from being saved:</h2>

      <ul>
      <% @theme_idea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :theme_id %><br>
    <%= f.number_field :theme_id %>
  </div>
  <div class="field">
    <%= f.label :idea_id %><br>
    <%= f.number_field :idea_id %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
